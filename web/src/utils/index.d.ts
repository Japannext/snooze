/**
 * Typescript types and function signatures for all the external objects that do not have
 * a typescript definition.
**/
declare module '@coreui/utils/src' {
  export const getStyle: (property: string, element?: HTMLElement) => string
}
