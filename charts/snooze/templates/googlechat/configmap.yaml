---
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-googlechat-profiles"
  namespace: "{{ .Release.Namespace }}"
data:
  profiles.yaml: |
    # Configuration of mail profiles
    ---
    profiles:
    {{- .Values.googlechat.profiles | toYaml | nindent 4 }}
